var chai = require('chai');
var expect = chai.expect;

var Vaada = require('../src/vaada');

// describe('Vaada', function() {
//   it('can be constructed and used as an object', function () {
//     var promise = new Vaada();
//     promise.aProperty = 1;
//     expect(promise.aProperty).to.equal(1);
//   });

//   it('should have an internal state property which should be initially set to "PENDING"', function () {
//     var promise = new Vaada();
//     expect(promise._state).to.be.ok;
//     expect(promise._state).to.equal('PENDING');
//   });

//   it('should have a then property on the returned promise object', function () {
//     var promise = new Vaada();
//     expect(promise.then).to.be.ok;
//   });

//   it('should ')
//   // then invocation should return a new promise
// });

